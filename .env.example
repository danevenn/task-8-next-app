# Neon PostgreSQL — copia este archivo a .env.local y rellena los valores.
# DATABASE_URL: cadena POOLED (con -pooler en el host). La usa Next.js en runtime.
# DIRECT_URL: cadena DIRECTA (sin -pooler). La usa Prisma para migraciones.

DATABASE_URL=
DIRECT_URL=
